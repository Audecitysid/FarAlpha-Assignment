git clone https://github.com/Audecitysid/Simple-Webserver-node

sudo apt update

sudo apt upgrade

sudo apt install nodejs

sudo apt install npm 

go to the directory and run
npm install

node app.js
